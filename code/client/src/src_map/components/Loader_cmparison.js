import { TailSpin } from 'react-loader-spinner';

const Loader_cmparison = () => {
  return (
  <div style={{ textAlign: 'center', padding: '50px',alignSelf:'center' }}>
    <TailSpin color="#00BFFF" height={80} width={80} />
  </div>
  )
};
export default Loader_cmparison;
